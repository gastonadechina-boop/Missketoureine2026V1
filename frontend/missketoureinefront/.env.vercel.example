# ⛔ FICHIER DÉSACTIVÉ — ENVIRONNEMENT LOCAL UNIQUEMENT
#
# Cet exemple était destiné au déploiement Vercel (production).
# Il a été neutralisé — aucune URL de production ne doit être utilisée ici.
#
# Pour le frontend local, utiliser uniquement :
#   VITE_API_URL=http://localhost:8000/api

# VITE_API_URL=https://...  ← désactivé
# VITE_API_TIMEOUT_MS=15000  ← désactivé
# VITE_MAX_VIDEO_UPLOAD_MB=100  ← désactivé
